{% extends 'base.html.twig' %}

{% block title %}Modifier la série{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier la série</h1>

        {{ include('program/_form.html.twig', {'button_label': 'Appliquer'}) }}
        <a href="{{ path('program_index') }}">
            <button class="btn btn-dark">Retour</button>
        </a>
        {{ include('program/_delete_form.html.twig') }}
    </div>
{% endblock %}
